# generated by fastapi-codegen:
#   filename:  openapi.yaml
#   timestamp: 2025-06-28T17:10:37+00:00



import argparse
import json
import os
from typing import *
from typing import Optional

from autogen.mcp.mcp_proxy import MCPProxy
from autogen.mcp.mcp_proxy.security import APIKeyHeader, BaseSecurity, HTTPBasic
from fastapi import Path, Query
from starlette.requests import Request

from models import (
    API2ControllersWebAPIGroceryListControllerDepartmentModel,
    API2ControllersWebAPIGroceryListControllerPostGroceryListAddLineRequest,
    API2ControllersWebAPIGroceryListControllerPostGroceryListRecipeRequest,
    API2ControllersWebAPIGroceryListControllerPostGroceryListSyncRequest,
    API2ControllersWebAPIGroceryListControllerPostToGroceryListRecipeRequest,
    API2ControllersWebAPIGroceryListControllerUpdateItemByGuidRequest,
    API2ControllersWebAPIImagesControllerRecipePhotosResponse,
    API2ControllersWebAPIMeControllerPreferenceOptions,
    API2ControllersWebAPINoteControllerNoteRequest,
    API2ControllersWebAPIReviewControllerPostReplyReq,
    API2ControllersWebAPIReviewControllerReviewRequest,
    API2ControllersWebAPIReviewControllerReviewRequestLegacy,
    API2ModelsBigOvenUser,
    API2ModelsPersonal,
    API2ModelsPreference,
    API2ModelsProfile,
    API2ModelsRecipesFeedbackDTO,
    API2ModelsRecipesRecipeResponse,
    API2Result,
    BigOvenModelAPI2CollectionInfo,
    BigOvenModelAPI2GroceryList,
    BigOvenModelAPI2Recipe,
    BigOvenModelAPI2RecipeNote,
    BigOvenModelAPI2RecipeSearchResult,
    BigOvenModelAPIRecipe,
    BigOvenModelAPIRecipeNote,
    BigOvenModelAPIRecipeNoteList,
    BigOvenModelAPIReply,
    BigOvenModelAPIReview,
    BigOvenModelShoppingListLine,
    BigOvenResult,
    CollectionsGetResponse,
    GrocerylistDepartmentPostResponse,
    RecipeAutocompleteAllGetResponse,
    RecipeAutocompleteGetResponse,
    RecipeAutocompleteMineGetResponse,
    RecipeCategoriesGetResponse,
    RecipeRecipeIdImagesGetResponse,
    RecipeRecipeIdReviewsGetResponse,
    RecipeRecipeIdScansGetResponse,
    RecipeReviewReviewIdRepliesGetResponse,
    RecipesRavesGetResponse,
    RecipesRecentviewsGetResponse,
    SystemObject,
)

app = MCPProxy(
    description='#Documentation\r\n\r\nThis is the documentation for the partner endpoint of the BigOven Recipe and Grocery List API.\r\n\r\nThe update brings with it Swagger-based documentation. [Swagger](http://swagger.io) is an emerging standard for describing REST-based APIs, and with this Swagger-compliant endpoint (above), you can make ready-to-go interface libraries for your code via [swagger-codegen](https://github.com/swagger-api/swagger-codegen). For instance, it\'s easy to generate libraries for Node.js, Java, Ruby, ASP.NET MVC, jQuery, php and more!\r\n\r\nYou can also try out the endpoint calls with your own api_key right here on this page. Be sure to enter your api_key above to use the "Try it out!" buttons on this page.\r\n\r\n##Start Here\r\n\r\nDevelopers new to the BigOven API should start with this version, not with the legacy API. We\'ll be making improvements to this API over time, and doing only bug fixes on the v1 API.\r\n\r\n\r\n\r\nTo pretend you\'re a BigOven user (for instance, to get your recently viewed recipes or your grocery list), you need to pass in Basic Authentication information in the header, just as with the v1 API. We do now require that you make all calls via https. You need to pass your api_key in with every call, though this can now be done on the header (send a request header "X-BigOven-API-Key" set to your api_key value, e.g., Request["X-BigOven-API-Key"]="your-key-here".)\r\n\r\n##Migration Notes\r\n\r\nFor existing partners, we encourage you to [migrate](https://api2.bigoven.com), and while at this writing we have no hard-and-fast termination date for the v1 API, we strongly prefer that you migrate by January 1, 2017. While the changes aren\'t overly complex, there are several breaking changes, including refactoring of recipe search and results and removal of support for XML. This is not a simply plug-and-play replacement to the v1 API. With respect to an exclusive focus on JSON, the world has spoken, and it prefers JSON for REST-based API\'s. We\'ve taken numerous steps to refactor the API to make it more REST-compliant. Note that this v2 API will be the preferred API from this point onward, so we encourage developers to migrate to this new format. We have put together some [migration notes](/web/documentation/migration-to-v2) that we encourage you to read carefully.\r\n\r\n##Photos\r\n\r\nSee our [photos documentation](https://api2.bigoven.com/web/documentation/recipe-images). \r\n\r\nFor more information on usage of this API, including features, pricing, rate limits, terms and conditions, please visit the [BigOven API website](https://api2.bigoven.com).',
    title='1,000,000+ Recipe and Grocery List API (v2)',
    version='partner',
    servers=[{'url': 'https://api2.bigoven.com'}],
)


@app.get('/collection/{id}', tags=['recipe_collection_management', 'recipe_operations'])
def collection__get_collection(
    id: int,
    rpp: Optional[int] = None,
    pg: Optional[int] = None,
    test: Optional[bool] = None,
    session_for_logging: Optional[str] = Query(None, alias='sessionForLogging'),
):
    """
    Gets a recipe collection. A recipe collection is a curated set of recipes.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/collection/{id}/meta', tags=['recipe_collection_management'])
def collection__get_collection_meta(id: int):
    """
    Gets a recipe collection metadata. A recipe collection is a curated set of recipes.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/collections', tags=['recipe_collection_management', 'recipe_operations'])
def collection__collections(test: Optional[str] = None):
    """
    Get the list of current, seasonal recipe collections. From here, you can use the /collection/{id} endpoint to retrieve the recipes in those collections.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.delete('/grocerylist', tags=['grocery_list_management'])
def grocery_list__delete():
    """
    Delete all the items on a grocery list; faster operation than a sync with deleted items.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/grocerylist', tags=['grocery_list_management', 'user_profile_management'])
def grocery_list__get():
    """
    Get the user's grocery list.  User is determined by Basic Authentication.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post('/grocerylist/clearcheckedlines', tags=['grocery_list_management'])
def grocery_list__grocery_list_remove_marked_items():
    """
    Clears the checked lines.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post('/grocerylist/department', tags=['grocery_list_management'])
def grocery_list__department(
    body: API2ControllersWebAPIGroceryListControllerDepartmentModel,
):
    """
    Departmentalize a list of strings -- used for ad-hoc grocery list item addition
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post('/grocerylist/item', tags=['grocery_list_management'])
def post_grocerylist_item(
    body: API2ControllersWebAPIGroceryListControllerPostToGroceryListRecipeRequest,
):
    """
    Add a single line item to the grocery list
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.delete('/grocerylist/item/{guid}', tags=['grocery_list_management'])
def grocery_list__delete_item_by_guid(guid: str):
    """
    /grocerylist/item/{guid}  DELETE will delete this item assuming you own it.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.put('/grocerylist/item/{guid}', tags=['grocery_list_management'])
def grocery_list__grocery_list_item_guid(
    guid: str,
    body: API2ControllersWebAPIGroceryListControllerUpdateItemByGuidRequest = ...,
):
    """
    Update a grocery item by GUID
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post('/grocerylist/line', tags=['grocery_list_management'])
def grocery_list__post(
    body: API2ControllersWebAPIGroceryListControllerPostGroceryListAddLineRequest,
):
    """
    Add a single line item to the grocery list
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post(
    '/grocerylist/recipe',
    tags=['grocery_list_management', 'recipe_collection_management'],
)
def grocery_list__add_recipe(
    body: API2ControllersWebAPIGroceryListControllerPostGroceryListRecipeRequest,
):
    """
    Add a Recipe to the grocery list.  In the request data, pass in recipeId, scale (scale=1.0 says to keep the recipe the same size as originally posted), markAsPending (true/false) to indicate that
            the lines in the recipe should be marked in a "pending" (unconfirmed by user) state.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post('/grocerylist/sync', tags=['grocery_list_management'])
def grocery_list__post_grocery_list_sync(
    body: API2ControllersWebAPIGroceryListControllerPostGroceryListSyncRequest,
):
    """
    Synchronize the grocery list.  Call this with a POST to /grocerylist/sync
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post(
    '/image/avatar', tags=['user_profile_management', 'image_association_management']
)
def images__upload_user_avatar():
    """
    POST: /image/avatar

            Testing with Postman (validated 11/20/2015):
            1) Remove the Content-Type header; add authentication information
            2) On the request, click Body and choose "form-data", then add a line item with "key" column set to "file" and on the right,
            change the type of the input from Text to File.  Browse and choose a JPG.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/me', tags=['recipe_creation_management', 'recipe_operations'])
def me__index():
    """
    Indexes this instance.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.put('/me', tags=['user_profile_management', 'user_action_management'])
def me__put_me(body: API2ModelsBigOvenUser):
    """
    Puts me.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.put('/me/personal', tags=['user_profile_management'])
def me__put_me_personal(body: API2ModelsPersonal):
    """
    Puts me personal.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.put('/me/preferences', tags=['user_profile_management'])
def me__put_me_preferences(body: API2ModelsPreference):
    """
    Puts me preferences.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/me/preferences/options',
    tags=['recipe_search_management', 'recipe_collection_management'],
)
def me__get_options():
    """
    Gets the options.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.put('/me/profile', tags=['user_profile_management'])
def put_me_profile(body: API2ModelsProfile):
    """
    Puts me.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/me/skinny', tags=['recipe_operations'])
def me__skinny():
    """
    Skinnies this instance.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post('/recipe', tags=['recipe_creation_management', 'recipe_operations'])
def recipe__post(body: BigOvenModelAPIRecipe):
    """
    Add a new recipe
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.put('/recipe', tags=['recipe_creation_management', 'recipe_operations'])
def recipe__put(body: BigOvenModelAPIRecipe):
    """
    Update a recipe
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/recipe/autocomplete', tags=['recipe_search_management', 'recipe_operations'])
def recipe__auto_complete(query: str, limit: Optional[int] = None):
    """
    Given a query, return recipe titles starting with query. Query must be at least 3 chars in length.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipe/autocomplete/all', tags=['recipe_creation_management', 'recipe_operations']
)
def recipe__auto_complete_all_recipes(query: str, limit: int = ...):
    """
    Automatics the complete all recipes.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipe/autocomplete/mine',
    tags=[
        'recipe_creation_management',
        'recipe_operations',
        'recipe_search_management',
    ],
)
def recipe__auto_complete_my_recipes(query: str, limit: int = ...):
    """
    Automatics the complete my recipes.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipe/categories',
    tags=['recipe_creation_management', 'recipe_search_management'],
)
def recipe__categories():
    """
    Get a list of recipe categories (the ID field can be used for include_cat in search parameters)
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipe/get/active/recipe', tags=['user_recipe_engagement', 'recipe_operations']
)
def recipe__get_active_recipe(user_name: str = Query(..., alias='userName')):
    """
    Returns last active recipe for the user
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post(
    '/recipe/get/saved/step',
    tags=['recipe_step_access', 'recipe_operations', 'recipe_detail_retrieval'],
)
def recipe__get_step(
    user_name: str = Query(..., alias='userName'),
    recipe_id: int = Query(..., alias='recipeId'),
    step_id: int = Query(..., alias='stepId'),
):
    """
    Gets recipe single step as text
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post(
    '/recipe/get/step/number',
    tags=['recipe_step_access', 'recipe_detail_retrieval', 'recipe_operations'],
)
def recipe__get_step_number(
    user_name: str = Query(..., alias='userName'),
    recipe_id: int = Query(..., alias='recipeId'),
):
    """
    Returns stored step number and number of steps in recipe
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/recipe/photos/pending', tags=['user_action_management'])
def images__get_pending_by_user():
    """
    Gets the pending by user.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post('/recipe/post/step', tags=['recipe_step_access', 'recipe_operations'])
def recipe__get_steps(
    user_name: str = Query(..., alias='userName'),
    recipe_id: int = Query(..., alias='recipeId'),
    step_id: int = Query(..., alias='stepId'),
):
    """
    Stores recipe step number and returns saved step data
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.delete(
    '/recipe/review/replies/{replyId}',
    tags=['review_management', 'user_action_management'],
)
def review__delete_reply(reply_id: str = Path(..., alias='replyId')):
    """
    DELETE a reply to a given review. Authenticated user must be the one who originally posted the reply.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.put(
    '/recipe/review/replies/{replyId}',
    tags=['review_management', 'user_action_management'],
)
def review__put_reply(
    reply_id: str = Path(..., alias='replyId'),
    body: API2ControllersWebAPIReviewControllerPostReplyReq = ...,
):
    """
    Update (PUT) a reply to a given review. Authenticated user must be the original one that posted the reply.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipe/review/{reviewId}', tags=['review_management', 'user_action_management']
)
def get_recipe_review__review_id(review_id: str = Path(..., alias='reviewId')):
    """
    Get a given review by string-style ID. This will return a payload with FeaturedReply, ReplyCount.
            Recommended display is to list top-level reviews with one featured reply underneath.
            Currently, the FeaturedReply is the most recent one for that rating.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.put('/recipe/review/{reviewId}', tags=['review_management'])
def review__put(
    review_id: str = Path(..., alias='reviewId'),
    body: API2ControllersWebAPIReviewControllerReviewRequest = ...,
):
    """
    Update a given top-level review.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/recipe/review/{reviewId}/replies', tags=['review_management'])
def review__get_replies(
    review_id: str = Path(..., alias='reviewId'),
    pg: Optional[int] = None,
    rpp: Optional[int] = None,
):
    """
    Get a paged list of replies for a given review.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post('/recipe/review/{reviewId}/replies', tags=['review_management'])
def review__post_reply(
    review_id: str = Path(..., alias='reviewId'),
    body: API2ControllersWebAPIReviewControllerPostReplyReq = ...,
):
    """
    POST a reply to a given review. The date will be set by server. Note that replies no longer have star ratings, only top-level reviews do.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post(
    '/recipe/scan',
    tags=[
        'image_association_management',
        'recipe_creation_management',
        'recipe_operations',
    ],
)
def recipe__scan(
    test: Optional[bool] = None,
    devicetype: Optional[str] = None,
    lat: Optional[float] = None,
    lng: Optional[float] = None,
):
    """
    POST an image as a new RecipeScan request
                1)  Fetch the filename -- DONE
                2)  Copy it to the pics/scan folder - ENSURE NO NAMING COLLISIONS -- DONE
                3)  Create 120 thumbnail size  in pics/scan/120 -- DONE
                4)  Insert the CloudTasks record
                5)  Create the HIT
                6)  Update the CloudTasks record with the HIT ID
                7)  Email the requesing user
                8)  Call out to www.bigoven.com to fetch the image and re-create the thumbnail
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipe/steps/{id}',
    tags=['recipe_operations', 'recipe_detail_retrieval', 'recipe_step_access'],
)
def recipe__get_recipe_with_steps(id: int, prefetch: Optional[bool] = None):
    """
    Return full Recipe detail with steps. Returns 403 if the recipe is owned by someone else.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.delete('/recipe/{id}', tags=['recipe_operations', 'recipe_creation_management'])
def recipe__delete(id: int):
    """
    Delete a Recipe (you must be authenticated as an owner of the recipe)
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/recipe/{id}', tags=['recipe_operations', 'recipe_detail_retrieval'])
def recipe__get(id: int, prefetch: Optional[bool] = None):
    """
    Return full Recipe detail. Returns 403 if the recipe is owned by someone else.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/recipe/{id}/zap', tags=['recipe_operations', 'recipe_creation_management'])
def recipe__zap_recipe(id: int):
    """
    Zaps the recipe.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post(
    '/recipe/{recipeId}/feedback', tags=['review_management', 'recipe_operations']
)
def recipe__feedback(
    recipe_id: int = Path(..., alias='recipeId'),
    body: API2ModelsRecipesFeedbackDTO = ...,
):
    """
    Feedback on a Recipe -- for internal BigOven editors
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post(
    '/recipe/{recipeId}/image',
    tags=[
        'recipe_creation_management',
        'image_association_management',
        'recipe_operations',
    ],
)
def images__upload_recipe_image(
    recipe_id: str = Path(..., alias='recipeId'),
    caption: Optional[str] = None,
    lat: Optional[float] = None,
    lng: Optional[float] = None,
):
    """
    POST: /recipe/{recipeId}/image?lat=42&amp;lng=21&amp;caption=this%20is%20my%20caption

             Note that caption, lng and lat are all optional, but must go on the request URI as params because this endpoint
             needs a multipart/mime content header and will not parse JSON in the body along with it.

             Testing with Postman (validated 11/20/2015):
             1) Remove the Content-Type header; add authentication information
             2) On the request, click Body and choose "form-data", then add a line item with "key" column set to "file" and on the right,
             change the type of the input from Text to File.  Browse and choose a JPG.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipe/{recipeId}/images',
    tags=['image_association_management', 'recipe_detail_retrieval'],
)
def images__get(recipe_id: int = Path(..., alias='recipeId')):
    """
    Get all the images for a recipe. DEPRECATED. Please use /recipe/{recipeId}/photos.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post('/recipe/{recipeId}/note', tags=['note_management', 'recipe_operations'])
def note__post(
    recipe_id: int = Path(..., alias='recipeId'),
    body: API2ControllersWebAPINoteControllerNoteRequest = ...,
):
    """
    HTTP POST a new note into the system.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.delete(
    '/recipe/{recipeId}/note/{noteId}', tags=['review_management', 'note_management']
)
def note__delete(
    recipe_id: int = Path(..., alias='recipeId'),
    note_id: int = Path(..., alias='noteId'),
):
    """
    Delete a review
                do a DELETE Http request of /note/{ID}
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/recipe/{recipeId}/note/{noteId}', tags=['note_management'])
def note__get(
    recipe_id: int = Path(..., alias='recipeId'),
    note_id: int = Path(..., alias='noteId'),
):
    """
    Get a given note. Make sure you're passing authentication information in the header for the user who owns the note.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.put(
    '/recipe/{recipeId}/note/{noteId}', tags=['recipe_operations', 'note_management']
)
def note__put(
    recipe_id: int = Path(..., alias='recipeId'),
    note_id: int = Path(..., alias='noteId'),
    body: API2ControllersWebAPINoteControllerNoteRequest = ...,
):
    """
    HTTP PUT (update) a Recipe note (RecipeNote).
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipe/{recipeId}/notes', tags=['note_management', 'recipe_detail_retrieval']
)
def note__get_notes(
    recipe_id: int = Path(..., alias='recipeId'),
    pg: Optional[int] = None,
    rpp: Optional[int] = None,
):
    """
    recipe/100/notes
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipe/{recipeId}/photos',
    tags=['image_association_management', 'recipe_detail_retrieval'],
)
def images__get_recipe_photos(
    recipe_id: int = Path(..., alias='recipeId'),
    pg: Optional[int] = None,
    rpp: Optional[int] = None,
):
    """
    Get all the photos for a recipe
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipe/{recipeId}/related', tags=['recipe_detail_retrieval', 'recipe_operations']
)
def recipe__related(
    recipe_id: int = Path(..., alias='recipeId'),
    pg: Optional[int] = None,
    rpp: Optional[int] = None,
):
    """
    Get recipes related to the given recipeId
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipe/{recipeId}/review', tags=['review_management', 'user_action_management']
)
def get_recipe__recipe_id_review(recipe_id: int = Path(..., alias='recipeId')):
    """
    Get *my* review for the recipe {recipeId}, where "me" is determined by standard authentication headers
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.post('/recipe/{recipeId}/review', tags=['review_management'])
def review__post(
    recipe_id: int = Path(..., alias='recipeId'),
    body: API2ControllersWebAPIReviewControllerReviewRequest = ...,
):
    """
    Add a new review. Only one review can be provided per {userId, recipeId} pair. Otherwise your review will be updated.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.delete(
    '/recipe/{recipeId}/review/{reviewId}',
    tags=['review_management', 'recipe_operations'],
)
def review__delete(
    recipe_id: int = Path(..., alias='recipeId'),
    review_id: int = Path(..., alias='reviewId'),
):
    """
    DEPRECATED! - Deletes a review by recipeId and reviewId. Please use recipe/review/{reviewId} instead.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/recipe/{recipeId}/review/{reviewId}', tags=['review_management'])
def review__get(
    review_id: int = Path(..., alias='reviewId'),
    recipe_id: int = Path(..., alias='recipeId'),
):
    """
    Get a given review - DEPRECATED. See recipe/review/{reviewId} for the current usage.
            Beginning in January 2017, BigOven moded from an integer-based ID system to a GUID-style string-based ID system for reviews and replies.
            We are also supporting more of a "Google Play" style model for Reviews and Replies. That is, there are top-level Reviews and then
            an unlimited list of replies (which do not carry star ratings) underneath existing reviews. Also, a given user can only have one review
            per recipe. Existing legacy endpoints will continue to work, but we strongly recommend you migrate to using the newer endpoints listed
            which do NOT carry the "DEPRECATED" flag.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.put(
    '/recipe/{recipeId}/review/{reviewId}',
    tags=['review_management', 'recipe_operations'],
)
def review__put_legacy(
    review_id: int = Path(..., alias='reviewId'),
    recipe_id: int = Path(..., alias='recipeId'),
    body: API2ControllersWebAPIReviewControllerReviewRequestLegacy = ...,
):
    """
    HTTP PUT (update) a recipe review. DEPRECATED. Please see recipe/review/{reviewId} PUT for the new endpoint.
            We are moving to a string-based primary key system, no longer integers, for reviews and replies.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/recipe/{recipeId}/reviews', tags=['review_management'])
def review__get_reviews(
    recipe_id: int = Path(..., alias='recipeId'),
    pg: Optional[int] = None,
    rpp: Optional[int] = None,
):
    """
    Get paged list of reviews for a recipe. Each review will have at most one FeaturedReply, as well as a ReplyCount.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipe/{recipeId}/scans',
    tags=['image_association_management', 'recipe_detail_retrieval'],
)
def images__get_scan_images(recipe_id: int = Path(..., alias='recipeId')):
    """
    Gets a list of RecipeScan images for the recipe. There will be at most 3 per recipe.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipes',
    tags=[
        'recipe_search_management',
        'recipe_operations',
        'recipe_creation_management',
    ],
)
def recipe__recipe_search(
    any_kw: Optional[str] = None,
    folder: Optional[str] = None,
    coll: Optional[int] = None,
    filter: Optional[str] = None,
    title_kw: Optional[str] = None,
    user_id: Optional[int] = Query(None, alias='userId'),
    username: Optional[str] = None,
    token: Optional[str] = None,
    photos: Optional[bool] = None,
    boostmine: Optional[bool] = None,
    include_cat: Optional[str] = None,
    exclude_cat: Optional[str] = None,
    include_primarycat: Optional[str] = None,
    exclude_primarycat: Optional[str] = None,
    include_ing: Optional[str] = None,
    exclude_ing: Optional[str] = None,
    cuisine: Optional[str] = None,
    db: Optional[str] = None,
    userset: Optional[str] = None,
    servings_min: Optional[float] = Query(None, alias='servingsMin'),
    total_mins: Optional[int] = Query(None, alias='totalMins'),
    max_ingredients: Optional[int] = Query(None, alias='maxIngredients'),
    min_ingredients: Optional[int] = Query(None, alias='minIngredients'),
    rpp: Optional[int] = None,
    pg: Optional[int] = None,
    vtn: Optional[int] = None,
    vgn: Optional[int] = None,
    chs: Optional[int] = None,
    glf: Optional[int] = None,
    ntf: Optional[int] = None,
    dyf: Optional[int] = None,
    sff: Optional[int] = None,
    slf: Optional[int] = None,
    tnf: Optional[int] = None,
    wmf: Optional[int] = None,
    rmf: Optional[int] = None,
    cps: Optional[int] = None,
    champion: Optional[int] = None,
    synonyms: Optional[bool] = None,
):
    """
    Search for recipes. There are many parameters that you can apply. Starting with the most common, use title_kw to search within a title.
            Use any_kw to search across the entire recipe.
            If you'd like to limit by course, set the parameter "include_primarycat" to one of (appetizers,bread,breakfast,dessert,drinks,maindish,salad,sidedish,soup,marinades,other).
            If you'd like to exclude a category, set exclude_cat to one or more (comma-separated) list of those categories to exclude.
            If you'd like to include a category, set include_cat to one or more (comma-separated) of those categories to include.
            To explicitly include an ingredient in your search, set the parameter "include_ing" to a CSV of up to three ingredients, e.g.:include_ing=mustard,chicken,beef%20tips
            To explicitly exclude an ingredient in your search, set the parameter "exclude_ing" to a CSV of up to three ingredients.
            All searches must contain the paging parameters pg and rpp, which are integers, and represent the page number (1-based) and results per page (rpp).
            So, to get the third page of a result set paged with 25 recipes per page, you'd pass pg=3&amp;rpp=25
            If you'd like to target searches to just a single target user's recipes, set userId=the target userId (number).
            Or, you can set username=theirusername
            vtn;vgn;chs;glf;ntf;dyf;sff;slf;tnf;wmf;rmf;cps
            cuisine
            photos
            filter=added,try,favorites,myrecipes\r\n\r\n
            folder=FolderNameCaseSensitive
            coll=ID of Collection
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/recipes/random', tags=['random_recipe_retrieval', 'recipe_operations'])
def recipe__get_random_recipe():
    """
    Get a random, home-page-quality Recipe.
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipes/raves',
    tags=['recipe_operations', 'review_management', 'user_recipe_engagement'],
)
def recipe__raves(pg: Optional[int] = None, rpp: Optional[int] = None):
    """
    Get the recipe/comment tuples for those recipes with 4 or 5 star ratings
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/recipes/recentviews',
    tags=['user_recipe_engagement', 'recipe_collection_management'],
)
def recipe__recent_views(pg: Optional[int] = None, rpp: Optional[int] = None):
    """
    Get a list of recipes that the authenticated user has most recently viewed
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/recipes/top25random', tags=['recipe_search_management', 'recipe_operations'])
def recipe__recipe_search_random(
    any_kw: Optional[str] = None,
    folder: Optional[str] = None,
    coll: Optional[int] = None,
    filter: Optional[str] = None,
    title_kw: Optional[str] = None,
    user_id: Optional[int] = Query(None, alias='userId'),
    username: Optional[str] = None,
    token: Optional[str] = None,
    photos: Optional[bool] = None,
    boostmine: Optional[bool] = None,
    include_cat: Optional[str] = None,
    exclude_cat: Optional[str] = None,
    include_primarycat: Optional[str] = None,
    exclude_primarycat: Optional[str] = None,
    include_ing: Optional[str] = None,
    exclude_ing: Optional[str] = None,
    cuisine: Optional[str] = None,
    db: Optional[str] = None,
    userset: Optional[str] = None,
    servings_min: Optional[float] = Query(None, alias='servingsMin'),
    total_mins: Optional[int] = Query(None, alias='totalMins'),
    max_ingredients: Optional[int] = Query(None, alias='maxIngredients'),
    min_ingredients: Optional[int] = Query(None, alias='minIngredients'),
    vtn: Optional[int] = None,
    vgn: Optional[int] = None,
    chs: Optional[int] = None,
    glf: Optional[int] = None,
    ntf: Optional[int] = None,
    dyf: Optional[int] = None,
    sff: Optional[int] = None,
    slf: Optional[int] = None,
    tnf: Optional[int] = None,
    wmf: Optional[int] = None,
    rmf: Optional[int] = None,
    cps: Optional[int] = None,
    champion: Optional[int] = None,
    synonyms: Optional[bool] = None,
):
    """
    Search for recipes. There are many parameters that you can apply. Starting with the most common, use title_kw to search within a title.
            Use any_kw to search across the entire recipe.
            If you'd like to limit by course, set the parameter "include_primarycat" to one of (appetizers,bread,breakfast,dessert,drinks,maindish,salad,sidedish,soup,marinades,other).
            If you'd like to exclude a category, set exclude_cat to one or more (comma-separated) list of those categories to exclude.
            If you'd like to include a category, set include_cat to one or more (comma-separated) of those categories to include.
            To explicitly include an ingredient in your search, set the parameter "include_ing" to a CSV of up to three ingredients, e.g.:include_ing=mustard,chicken,beef%20tips
            To explicitly exclude an ingredient in your search, set the parameter "exclude_ing" to a CSV of up to three ingredients.
            All searches must contain the paging parameters pg and rpp, which are integers, and represent the page number (1-based) and results per page (rpp).
            So, to get the third page of a result set paged with 25 recipes per page, you'd pass pg=3&amp;rpp=25
            If you'd like to target searches to just a single target user's recipes, set userId=the target userId (number).
            Or, you can set username=theirusername
            vtn;vgn;chs;glf;ntf;dyf;sff;slf;tnf;wmf;rmf;cps
            cuisine
            photos
            filter=added,try,favorites,myrecipes\r\n\r\n
            folder=FolderNameCaseSensitive
            coll=ID of Collection
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get('/recipes/{id}', tags=['recipe_detail_retrieval', 'recipe_operations'])
def recipe__get_v2(id: int, prefetch: Optional[bool] = None):
    """
    Same as GET recipe but also includes the recipe videos (if any)
    """
    raise RuntimeError("Should be patched by MCPProxy and never executed")


if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="MCP Server")
    parser.add_argument(
        "transport",
        choices=["stdio", "sse", "streamable-http"],
        help="Transport mode (stdio, sse or streamable-http)",
    )
    args = parser.parse_args()

    if "CONFIG_PATH" in os.environ:
        config_path = os.environ["CONFIG_PATH"]
        app.load_configuration(config_path)

    if "CONFIG" in os.environ:
        config = os.environ["CONFIG"]
        app.load_configuration_from_string(config)

    if "SECURITY" in os.environ:
        security_params = BaseSecurity.parse_security_parameters_from_env(
            os.environ,
        )

        app.set_security_params(security_params)

    mcp_settings = json.loads(os.environ.get("MCP_SETTINGS", "{}"))

    app.get_mcp(**mcp_settings).run(transport=args.transport)
